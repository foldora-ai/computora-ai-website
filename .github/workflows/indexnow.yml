name: Notify IndexNow

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "scripts/submit-indexnow.mjs"
      - ".github/workflows/indexnow.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: indexnow
  cancel-in-progress: true

jobs:
  notify:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Submit sitemap URLs
        run: node scripts/submit-indexnow.mjs https://computoraai.com 5c32874288e54d57a720fecd0b25da46 docs/sitemap.xml
